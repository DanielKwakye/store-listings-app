import 'package:flutter/material.dart';

class KYCSubPage extends StatelessWidget {
  const KYCSubPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('KYC sub page'),
    );
  }
}
